#include "main.h"
/**
 * add - dds two integers
 * @x: One variable to be added
 * @y: The other variable to be added to get the sum
 * Return: result of adding two integers
 *
 */
int add(int x, int  y)
{
	int sum = x + y;

	return (sum);
}
