#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @i: The number used
 * Return: 1 or 0 if positive or zero
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
